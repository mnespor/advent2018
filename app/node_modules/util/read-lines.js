'use strict'

const fs = require('fs')
const R = require('ramda')

const splitLines = R.split('\n')
const readUtf8 = R.partialRight(fs.readFileSync, [{ encoding: 'utf8' }])

module.exports = R.pipe(readUtf8, splitLines)
